{
  "name": "anthonyec.github.io",
  "version": "1.0.0",
  "description": "My website, my rulez!!",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=dev node --enable-source-maps ./node_modules/.bin/staticbuild ./src ./dist --watch",
    "build": "git submodule update --init --recursive --depth=1 && node --enable-source-maps ./node_modules/.bin/staticbuild ./src ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anthonyec/website.git"
  },
  "author": "Anthony Cossins",
  "homepage": "https://github.com/anthonyec/website#readme",
  "devDependencies": {
    "@anthonyec/staticbuild": "github:anthonyec/staticbuild"
  }
}
